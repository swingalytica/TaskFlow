import Root from './button.svelte';

export { Root as Button, Root };

	export {
		buttonVariants,
		type ButtonProps,
		type ButtonSize,
		type ButtonVariant,
		type ButtonProps as Props
	} from './button.svelte';

